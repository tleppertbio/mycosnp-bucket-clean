# mycosnp-bucket-clean
Python code pushes sample processing scripts to google vms running a docker image tleppertwood/pathogentotree.  This code cleans up the bucket, checks the queues, requeues interrupted runs.
